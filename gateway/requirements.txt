flask==3.0.2
requests==2.32.3
pytest==8.3.2
# PyJWT and cryptography are present so the in-container test suite
# can construct rogue tokens for adversarial identity tests. The
# gateway service code itself does NOT mint or verify JWTs at runtime.
PyJWT==2.9.0
cryptography==43.0.1
